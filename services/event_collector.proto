syntax = "proto3";

package collector;

import "entities/funnel.proto";

service EventCollector {
  rpc TrackFunnelEvent (FunnelEvent) returns (Response) {}
}

message Response {
  string message = 1;
}
